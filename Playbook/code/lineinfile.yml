---
- name: set timezone in envirnement
  hosts: web1
  become: true
  tasks:
    - name: set timezone in web1
      lineinfile:
        path: /etc/environment
        line: 'TZ=Asia/kolkata'
        create: true
